<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <!--link_to 表示文字列, リンク先-->
  <!--リンク先としてインスタンス（レコード）を渡すと、自動的にそのインスタンスの show アクションへとリンクされる-->
    <li><%= link_to task.id, task %> : <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>